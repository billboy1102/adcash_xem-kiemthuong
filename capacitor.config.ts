import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.bobbey.adcash',
  appName: 'Adcash: Xem & Kiếm Thưởng',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
